services:
  postgres:
    image: postgres:16
    container_name: weather_pipeline_postgres
    environment:
      POSTGRES_DB: weather_pipeline_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: